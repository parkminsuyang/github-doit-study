$ pwd #입력시 현재 위치 경로 나타남
$ ls #디렉터리와 파일 이름이 나타남
$ clear #터미널 창 지우기
$ cd .. #터미널 창에서 디렉터리 이동하기
$ cd users #하위 디렉터리로 이동
$ cd Documents
$ mkdir test #Documents 디렉터리에 test라는 하위 디렉터리 생성
$ rm -r test #test 디렉터리 삭제
$ exit #터미널 종료
